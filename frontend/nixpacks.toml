# Configuration Nixpacks pour le Frontend Flutter
[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = [
    "npm install -g @flutter/cli",
    "flutter pub get"
]

[phases.build]
cmds = [
    "flutter build web --release"
]

[start]
cmd = "cd build/web && python -m http.server 3000"