{
  "name": "dons-frontend",
  "version": "1.0.0",
  "description": "Frontend Flutter pour l'application DONS",
  "scripts": {
    "build": "flutter build web --release",
    "dev": "flutter run -d web-server --web-port 3000",
    "install": "flutter pub get"
  },
  "dependencies": {
    "flutter": "^3.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
